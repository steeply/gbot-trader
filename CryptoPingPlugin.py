#!/usr/bin/env python3
from telethon import TelegramClient
from telethon.tl.functions.messages import SendMessageRequest
from telethon.tl import types, functions



def dat(update):
    data = str(update)
    if "#AMP" in data:
        print("A AMP WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/AMP'))
    if "#ARDR" in data:
        print("A ARDR WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/ARDR'))
    if "#BCH" in data:
        print("A BCH WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BCH'))
    if "#BCN" in data:
        print("A BCN WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BCN'))
    if "#BCY" in data:
        print("A BCY WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BCY'))
    if "#BELA" in data:
        print("A BELA WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BELA'))
    if "#BLK" in data:
        print("A BLK WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BLK'))
    if "#BTCD" in data:
        print("A BTCD WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BTCD'))
    if "#BTM" in data:
        print("A BTM WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BTM'))
    if "#BTS" in data:
        print("A BTS WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BTS'))
    if "#BURST" in data:
        print("A BURST WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/BURST'))
    if "#CLAM" in data:
        print("A CLAM WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/CLAM'))
    if "#CVC" in data:
        print("A CVC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/CVC'))
    if "#DASH" in data:
        print("A DASH WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/DASH'))
    if "#DCR" in data:
        print("A DCR WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/DCR'))
    if "#DGB" in data:
        print("A DGB WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade DGB/AMP'))
    if "#DOGE" in data:
        print("A DOGE WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/DOGE'))
    if "#EMC2" in data:
        print("A EMC2 WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/EMC2'))
    if "#ETC" in data:
        print("A ETC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/ETC'))
    if "#ETH" in data:
        print("A ETH WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/ETH'))
    if "#EXP" in data:
        print("A EXP WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/EXP'))
    if "#FCT" in data:
        print("A FCT WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/FCT'))
    if "#FLDC" in data:
        print("A FLDC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/FLDC'))
    if "#FLO" in data:
        print("A FLO WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/FLO'))
    if "#GAME" in data:
        print("A GAME WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/GAME'))
    if "#GAS" in data:
        print("A GAS WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/GAS'))
    if "#GNO" in data:
        print("A GNO WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/GNO'))
    if "#GNT" in data:
        print("A GNT WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/GNT'))
    if "#GRC" in data:
        print("A GRC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/GRC'))
    if "#HUC" in data:
        print("A HUC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/HUC'))
    if "#LBC" in data:
        print("A LBC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/LBC'))
    if "#LSK" in data:
        print("A LSK WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/LSK'))
    if "#LTC" in data:
        print("A LTC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/LTC'))
    if "#MAID" in data:
        print("A MAID WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/MAID'))
    if "#NAV" in data:
        print("A NAV WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/NAV'))
    if "#NEOS" in data:
        print("A NEOS WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/NEOS'))
    if "#NMC" in data:
        print("A NMC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/NMC'))
    if "#NXC" in data:
        print("A NXC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/NXC'))
    if "#NXT" in data:
        print("A NXT WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/NXT'))
    if "#OMG" in data:
        print("A OMG WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/OMG'))
    if "#OMNI" in data:
        print("A OMNI WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/OMNI'))
    if "#PASC" in data:
        print("A PASC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/PASC'))
    if "#PINK" in data:
        print("A PINK WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/PINK'))
    if "#POT" in data:
        print("A POT WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/POT'))
    if "#PPC" in data:
        print("A PPC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/PPC'))
    if "#RADS" in data:
        print("A RADS WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/RADS'))
    if "#REP" in data:
        print("A REP WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/REP'))
    if "#RIC" in data:
        print("A RIC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/RIC'))
    if "#SBD" in data:
        print("A SBD WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/SBD'))
    if "#SC" in data:
        print("A SC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/SC'))
    if "#STEEM" in data:
        print("A STEEM WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/STEEM'))
    if "#STORJ" in data:
        print("A STORJ WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/STORJ'))
    if "#STR" in data:
        print("A STR WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/STR'))
    if "#STRAT" in data:
        print("A STRAT WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/STRAT'))
    if "#SYS" in data:
        print("A SYS WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/SYS'))
    if "#VIA" in data:
        print("A VIA WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/VIA'))
    if "#VRC" in data:
        print("A VRC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/VRC'))
    if "#VTC" in data:
        print("A VTC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/VTC'))
    if "#XBC" in data:
        print("A XBC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XBC'))
    if "#XCP" in data:
        print("A XCP WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XCP'))
    if "#XEM" in data:
        print("A XEM WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XEM'))
    if "#XMR" in data:
        print("A XMR WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XMR'))
    if "#XPM" in data:
        print("A XPM WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XPM'))
    if "#XRP" in data:
        print("A XRP WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XRP'))
    if "#XVC" in data:
        print("A XVC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/XVC'))
    if "#ZEC" in data:
        print("A ZEC WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/ZEC'))
    if "#ZRX" in data:
        print("A ZRX WAS FOUND!!!")
        peer = client.get_input_entity('smellysmellysmellybot')
        result = client(SendMessageRequest(peer, '/sell_all'))
        result = client(SendMessageRequest(peer, '/trade BTC/ZRX'))
data = "null"
def load_settings(path='api/settings'):
    """Loads the user settings located under `api/`"""
    result = {}
    with open(path, 'r', encoding='utf-8') as file:
        for line in file:
            value_pair = line.split('=')
            left = value_pair[0].strip()
            right = value_pair[1].strip()
            if right.isnumeric():
                result[left] = int(right)
            else:
                result[left] = right

    return result

settings = load_settings()


session_name = "botbotbot"
client = TelegramClient(session_name,
                        int(settings['api_id']),
                        settings['api_hash'],
                        proxy=None,
                        update_workers=4,
                        spawn_read_thread=False)
if True:
    client.start(phone='+18325443691')
else:
    client.start()

client.add_update_handler(dat)
print('(Press Ctrl+C to stop this)')
client.idle()
